{
  "name": "dialsim",
  "source": "index.html",
  "version": "0.5.2",
  "description": "dialsim project",
  "scripts": {
    "start": "npx parcel",
    "build": "npx parcel build",
    "cypress:install": "npx cypress install",
    "cypress:run": "npx cypress run",
    "cypress:runwithcoverage": "npx nyc --reporter=lcov --reporter=text-summary cypress run",
    "cypress:rundrtimsuite": "npx cypress run --spec cypress/e2e/tmeyer_amlk.cy.js"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "chart.js": "4.4.2",
    "goal-seek": "^0.1.4",
    "lodash": "^4.17.21",
    "prettier": "^3.0.3"
  },
  "devDependencies": {
    "@cypress/code-coverage": "^3.12.1",
    "babel-plugin-istanbul": "^6.1.1",
    "cypress": "^13.1.0",
    "nyc": "^15.1.0",
    "parcel": "^2.12.0"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
