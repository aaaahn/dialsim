{
  "name": "Codespace for dialsim - default github image + xvfb",
  "dockerFile": "Dockerfile",
  "postCreateCommand": "npm install",
  "customizations": {
    "vscode": {
      "extensions":    [
        "cypress-io.cypress",  // Add any VS Code extensions you want pre-installed
        "ms-azuretools.vscode-docker"
      ]    
    }
  }
}
