{
  "name": "dialsim",
  "source": "index.html",
  "version": "0.1.1",
  "description": "dialsim project",
  "scripts": {
    "start": "npx parcel",
    "build": "npx parcel build",
    "cypress:install": "npx cypress install",
    "cypress:run": "npx nyc --reporter=lcov --reporter=text-summary cypress run"
  },
  "dependencies": {
    "chart.js": "4.4.0",
    "goal-seek": "^0.1.4"
  },
  "devDependencies": {
    "@cypress/code-coverage": "^3.12.0",
    "babel-plugin-istanbul": "^6.1.1",
    "cypress": "^13.1.0",
    "htmlnano": "2.0.3",
    "nyc": "^15.1.0",
    "parcel": "^2.9.3"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
